name: desafio_campo_minado
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  business:
    path: ./business/
  cloud_firestore: ^0.13.0+1
  firebase_core: ^0.4.3+2
  async_redux: ^2.5.0
  random_string: ^2.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  mockito: ^4.1.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true